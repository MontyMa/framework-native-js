import * as actions from './actions'
import * as getters from './getters'
import mutations from './mutations'

const state = {
  updateCnyList: false,
  updateDigistList: false,
  updataImgCode: false,
}

export default{
  state,
  actions,
  getters,
  mutations
}
